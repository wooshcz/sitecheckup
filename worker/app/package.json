{
  "name": "sitecheckup-worker",
  "description": "NodeJS worker which can execute Lighthouse, LinkChecker and TestSSL jobs and store the results in DB",
  "version": "1.0.0",
  "author": "Ondrej Moravek",
  "dependencies": {
    "fast-xml-parser": "^5.3.6",
    "lighthouse": "^12.8.0",
    "pg": "^8.16.3"
  },
  "license": "MIT",
  "main": "index.cjs",
  "scripts": {
    "dev": "nodemon index.cjs",
    "start": "node index.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
